export default function MapPlaceholder() {
  return (
    <div className="flex items-center justify-center bg-gray-200 h-full rounded-lg">
      <span className="text-gray-500">[ Bản đồ sẽ hiển thị ở đây ]</span>
    </div>
  );
}
